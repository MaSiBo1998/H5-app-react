import type { ReactElement } from 'react'

export default function Payment({ data }: { data?: any }): ReactElement {
  return <div style={{ padding: 20, textAlign: 'center' }}>Payment Page (Under Construction)</div>
}
